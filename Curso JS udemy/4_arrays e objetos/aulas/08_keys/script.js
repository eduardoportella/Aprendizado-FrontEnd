let obj = {
    'chave 1': 1, //*CHAVE DA PROPRIEDADE*: *VALOR DO OBJETO DA PROPRIEDADE*
    'chave 2': 2,
    'chave 3': 3
}

console.log(obj); //MOSTRA AS PROPRIEDADES

console.log('------')

console.log(Object.keys(obj)); //RECEBE EM ARRAY